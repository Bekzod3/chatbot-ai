# Chatbot Project

This is a simple chatbot built with Python. It responds to a few basic inputs like "hello", "how are you", and "bye".

## Installation

1. Clone this repository:
    ```bash
    git clone https://github.com/your-username/chatbot-project.git
    cd chatbot-project
    ```

2. Install dependencies (if you have a `requirements.txt`):
    ```bash
    pip install -r requirements.txt
    ```

## Usage

Run the chatbot by executing:

```bash
python chatbot.py
